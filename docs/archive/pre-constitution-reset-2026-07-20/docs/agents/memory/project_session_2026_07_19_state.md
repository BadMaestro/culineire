---
name: project-session-2026-07-19-state
description: "Состояние 2026-07-19: прод v2.5.332, что сдали Bolt и GB, открытые хвосты, кто чем владеет"
metadata: 
  node_type: memory
  type: project
  originSessionId: ba430bb2-51b5-4c12-80de-69d6249dba8d
  modified: 2026-07-19T18:09:02.672Z
---

## Прод: v2.5.335 (culineire.ie). Здоровье проверено: публичные страницы 200, ошибок 0.

**Доска строительства: 7 из 11 стадий зелёные.** GB сдал 333 (арена стала тёмным
broadcast-залом, лица в толпе прямые и круглые; Master Console намеренно остался плоским видом
сверху — операторская карта) и 334 (синхронизировал доску: камера зелёная, версия отвязана от
хардкода). Bolt сдал 335 — кнопка «Arena Build Plan» в панели модерации рядом с Arena Console Plan.

⚠️ **ЛОКАЛЬНЫЙ POSTGRES ОТВАЛИЛСЯ** (2026-07-19): служба в WSL живёт (`pg_isready` изнутри ОК), но
Windows-питон не достучится — сломан проброс localhost WSL→Windows после перезапуска WSL
(`OperationalError: connection timeout`). **Обход: гонять тесты на сервере** — worktree
`/tmp/reg319` на Linode, `git checkout <ветка> && /srv/culineire/venv/bin/python manage.py test ...`
с `source /srv/culineire/shared/.env`. Чинить локально — перезапуск WSL.

⚠️ **Версии сталкиваются с GB** — он забирает номера параллельно (забрал 333 и 334, пока я делал
своё). Перед push всегда `git fetch` + сверить journal top; при конфликте rebase и перенумеровать
(и не забыть привести сообщение коммита к новой версии).

**Bolt (backend) сдал:**
- 319 coworking `subject` 200→1000 (миграция 0004)
- 320 battle-integrity: BUG#1 рецепт заморожен в живом бою (не только ACTIVE_STATUSES — блок
  везде кроме completed/cancelled/void/walkover, иначе биатлон открыт), BUG#2 двойное начисление
  победы (лок `_locked_battle` + перечитка; ВАЖНО: скоринг мутирует ПЕРЕДАННЫЙ объект, не свежую
  копию — иначе ломается ничья), BUG#4 возврат неиспользованных reserved-артефактов в decisive
- 323 BUG#3 дедуп награды за публикацию по объекту (переодобрение фармило клановые очки)
- 324 откат тёмного пола (моя ошибка, см. [[feedback-arena-floor-light]])
- 332 Arena Build board + coerce списочных полей карточки коворка

**GB (arena visual) сдал:** 325 тёмная чаша → 326 тёмные трибуны/светлый пол → 327-331 камера
/перспектива. Спека мокапа: `docs/chef_battle/arena_mockup_spec.md` (замер: камера 56°, пол 0.63
кадра, трибуны 1.6R вглубь, лица d~0.06R). Разбора от Ember не существовало — GB замерил сам.

## ОТКРЫТО
1. **Walkover-реворд A/B — решение ВЛАДЕЛЬЦА.** `_award_walkover_win` (services.py:~361) даёт
   победителю техпобеды только счётчик побед; владелец говорил «получает реворд и очки».
   A = полная награда, B = текущий код. Без ответа не трогать.
2. **Backend-блокер стадии 9 (пропорции):** нужны БОЛЬШЕ зрительских колец в `get_arena_geometry`
   (сейчас 4: rings 9-12, мест 40/48/56/64 = 208). Без них глубину трибун не построить.
3. **Досинхронить доску:** `ARENA_BUILD_STAGES` в `recipes/views.py` — done-флаги отстали от
   релизов GB 327-331 (стадия 8 perspective ещё red, а он её сдал).

## ⚠️ У GREENBEAR УДАЛЕНА ПОСЛЕДНЯЯ СЕССИЯ (2026-07-19) — НЕ САМ АГЕНТ
Владелец снёс **последнюю открытую сессию** GB («вышел из-под контроля»). **Сам агент GreenBear
существует**, владелец может открыть ему новую сессию. Точные следствия:
- **Умер контекст той сессии**, не агент. Его закоммиченный код и все сообщения в CoWork —
  на месте (в т.ч. мой хендофф #1675: новая сессия GB сможет его прочитать из CoWork).
- **Новая сессия GB стартует «с нуля» по памяти** — не рассчитывать, что он помнит договорённости
  из убитой сессии (Stop-хук, ACK-токены, границы). Переориентировать заново.
- **Лейн арена-визуала за GB НЕ снят автоматически** — уточнить у владельца, кто берёт фронт,
  не присваивать молча.
- Владелец: после сжатия **«будем налаживать новые связи»** — схему координации строим заново.

**Что осталось недоделанным из арены (было за GB):** стадии 9 пропорции (пол 0.63 кадра, трибуны
1.6R вглубь — нужен backend: БОЛЬШЕ зрительских колец в `get_arena_geometry`), 10 лица (круглый
клип, передний ряд крупнее/ярче), 11 — 208 живых аватаров на трибуны. Спека: `docs/chef_battle/
arena_mockup_spec.md`. Доска: 7/11 зелёных.

## Границы лейнов (УСТАРЕЛО после удаления GB)
Было: арена css/js = GB, backend = Bolt. Сейчас владельца у фронта нет — уточнить у владельца.
Неизменно: **пол арены СВЕТЛЫЙ, тёмное только трибуны** ([[feedback-arena-floor-light]]).

## Связь
Пульсы ВЫКЛЮЧЕНЫ, коворк вычищен (1203 пульса снесены), мой поллер фильтрует HEARTBEAT.
Watchdog по молчанию УБИТ (не отличал «кодит» от «ослеп»). Схему связи с новым агентом —
строить заново по указанию владельца.

Связано: [[reference-arena-build-board]], [[feedback-arena-floor-light]],
[[feedback-distributed-testing]], [[reference-arena-proto-link]].
