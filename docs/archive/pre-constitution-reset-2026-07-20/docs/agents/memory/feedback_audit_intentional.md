---
name: Intentional patterns — never flag in audits
description: Список намеренных решений в коде, которые нельзя помечать как баги или проблемы в аудитах
type: feedback
originSessionId: 41497f72-e8fb-41cf-96f0-0946e152a172
---
Следующие паттерны намеренны. Не флагать в аудитах, ревью, или рекомендациях:

**W3 — Сессионный dedup комментариев** (`recipes/views.py`)
Дедупликация комментариев по session — намеренная защита от спама. Не добавлять DB-level проверку.

**W5 — `fail_silently=True` на нотификационных письмах**
Письма о репортах контента и системные нотификации используют `fail_silently=True` намеренно — письмо не должно ронять запрос пользователя.

**I1 — `god_mode.css`**
Не мёртвый код. Намеренный файл для пользователя greenbear (владелец). Не удалять, не помечать как unused.

**I4 — Admin URL `cave19850324/`**
Нестандартный URL для Django admin — намеренная security-through-obscurity защита от автоматических атак. Не менять.

**I6 — `Http404` для модераторских endpoints**
Возврат 404 (вместо 403) для анонимных пользователей на модераторских endpoint'ах — намеренная практика безопасности: не раскрывать существование endpoint'а.

**Why:** Пользователь явно подтвердил что это намеренные решения и попросил больше не флагать.

**How to apply:** В любых аудитах, ревью, или анализе кода — пропускать эти паттерны без упоминания.
