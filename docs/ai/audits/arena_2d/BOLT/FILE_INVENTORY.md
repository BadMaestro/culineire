# File Inventory — Backend and Domain Audit (BOLT)

Scope: Python backend files under `chef_battle/` reachable from the Arena
and Chef Battle domain. Analysis only — no file was modified.

| File | Purpose | Loaded or called by | Calls or loads | Current responsibility | 3D-specific | Reusable | Duplicate candidate | Dead-code status | Evidence | Recommended future action |
|---|---|---|---|---|---|---|---|---|---|---|
| `chef_battle/models.py` (2045 lines) | All domain models: profiles, challenges, battles, entries, votes, events, artifacts, gifts, tokens, seasons, clans, factions, reports | Every other backend file in the app; Django ORM/admin/migrations | `recipes.models` (Recipe, RecipeAuthor), `articles.models` (Article), `django.conf.settings` | Data schema and DB-level integrity (constraints, indexes) | No | Yes | No | Active | `chef_battle/models.py:1-2045`; `chef_battle/migrations/0083_*` applies a constraint from this file, confirmed via `makemigrations --check --dry-run` returning "No changes detected" against this exact commit | KEEP_UNCHANGED |
| `chef_battle/services.py` (3393 lines) | Business logic: challenge accept/refuse/expiry, entry submission, reveal, vote hashing, battle result/rating, crown award, gifts, artifacts, payouts | `chef_battle/views.py`, `chef_battle/tests.py`, management commands | `chef_battle/models.py`, `chef_battle/fraud.py`, `newsfeed.models`, `django.db.transaction` | The single procedural business-logic layer (no logic duplicated into models per the manifest's stated architecture triad) | No | Yes | No | Active | `accept_challenge` at `services.py:170`, `calculate_battle_result` at `services.py:621`, crown award at `services.py:690-691`, `hash_request_value` at `services.py:84` (rewritten this session to HMAC) | KEEP_UNCHANGED |
| `chef_battle/selectors.py` (1319 lines) | Read-only query layer: arena geometry, phase, deadline, spectators, rankings, crown ladder, recent gifts | `chef_battle/views.py` | `chef_battle/models.py`, `django.db.models` (Count, F, Q, annotations) | Every read the arena page and its poll endpoint serve | No (declarative ring/segment counts, not a visual projection) | Yes | No | Active | `get_arena_geometry`, `spectator_capacity()`, `SPECTATOR_RING_SEGMENTS` (referenced earlier this session at `selectors.py:1245`) | KEEP_UNCHANGED — this is the exact contract a 2D frontend would consume; see EVIDENCE.md for the geometry-contract boundary analysis |
| `chef_battle/views.py` (3179 lines) | HTTP views: arena page, arena_state poll endpoint, battle detail, vote, challenge, console views, moderation | `chef_battle/urls.py` (76 `path()` entries) | `chef_battle/services.py`, `chef_battle/selectors.py`, `chef_battle/access.py`, `chef_battle/fraud.py` | Request/response boundary; template context assembly (`_arena_center`, `_build_arena_payload`) | Partial — `_arena_center` and the arena payload carry no 3D-specific data themselves, but the view is the sole place the frontend's projection inputs (geometry rings) originate from | Yes | No | Active | `arena` view at `views.py:1068`; `_arena_center` at `views.py:783` (verified this session to already return `challenger.name`/`avatar_url` for both fighters) | KEEP_BACKEND_REPLACE_PRESENTATION — the view functions and context contract stay; only the templates they render change |
| `chef_battle/access.py` (106 lines) | Two visibility gates: `is_battle_visible` (arena/dark-launch), `has_arena_console_access` (operator console, DG-01) | `chef_battle/views.py` (`chef_battle_guard`, `arena_console_guard` decorators) | `django.conf.settings` | Authorization boundary, independent of presentation | No | Yes | No | Active | `is_battle_visible` at `access.py:11-32` (modified this session, commit `38c352a4`, to add the "any registered author" branch per the owner's rule); `has_arena_console_access` at `access.py:34-53` | KEEP_UNCHANGED — this file is the exact boundary a 2D rebuild must not touch to avoid re-opening the access question |
| `chef_battle/fraud.py` (431 lines) | Anti-fraud gate pipeline: account age, vote rate, duplicate device, self-vote, participant-vote, suspended/flagged account | `chef_battle/views.py` (`run_fraud_gates` calls in `battle_vote`, challenge creation) | `chef_battle/models.py` (BattleVote, VoteIntegrityEvent queries) | Fraud/abuse prevention, independent of presentation | No | Yes | No | Active | `gate_self_vote` at `fraud.py:114`, `gate_duplicate_device` at `fraud.py:93`, both confirmed wired into `battle_vote` at `views.py:1586,1590` | KEEP_UNCHANGED |
| `chef_battle/arena_snapshot.py` (117 lines) | Not yet fully traced this pass | Unknown pending further review | Unknown pending further review | Unknown | Unknown | UNKNOWN | Unknown | UNKNOWN | Filename suggests an arena state snapshot mechanism, not yet cross-checked against `selectors.py`'s geometry functions for overlap | UNKNOWN_PENDING_RUNTIME_CHECK |
| `chef_battle/migrations/` (84 files, latest `0083_vote_self_vote_constraint_and_hash_scheme.py`) | Schema history | Django migration runner | `chef_battle/models.py` at each migration's point in history | Schema evolution record | No | Yes (as history) | No | Active | `makemigrations --check --dry-run` returned "No changes detected" against this exact base commit, confirming models.py and migrations are in sync | KEEP_UNCHANGED |
| `chef_battle/tests.py` (7111 lines, 465 `test_` methods) | Backend test suite | Django test runner | Every file above | Regression protection for all of the above | Some tests (not yet individually classified) may assert presentation details (e.g. arena geometry ring counts) rather than pure backend behaviour | Yes, selectively | No | Active | 465 `def test_` matches counted directly; this session independently ran 697 tests total (chef_battle + recipes + presence) at 100% pass against this exact commit lineage | KEEP_AND_REUSE for backend-asserting tests; CONSOLIDATE_LATER for any found to assert presentation specifics once CLAUDE_B's test-coverage pass is complete (see STATUS.yml dependencies_on_peers) |
| `chef_battle/emulation.py` (274 lines), `chef_battle/energy_service.py` (224), `chef_battle/stripe_services.py` (326), `chef_battle/observer_service.py`/`observer_views.py`, `chef_battle/clan_*.py`, `chef_battle/faction_*.py`, `chef_battle/season_service.py`/`season_signals.py`, `chef_battle/reaction_service.py`, `chef_battle/token_config.py`, `chef_battle/forms.py`, `chef_battle/admin.py` (889) | Adjacent subsystems (economy, seasons, clans, factions, live observers, Stripe, moderation forms/admin) | Various, not individually traced this pass | Various | Each a distinct subsystem outside the Arena's core visibility/voting/battle-lifecycle path this audit was scoped to first | No (not yet confirmed for each individually) | Presumed yes, not yet individually verified | Not yet checked | Presumed active (all import cleanly per `manage.py check` passing) | `manage.py check` passed with 0 issues across the full app, meaning none of these files has an import-time error, but this is not equivalent to confirming each is reachable/used at runtime | UNKNOWN_PENDING_RUNTIME_CHECK — flagged for a follow-up pass, not claimed dead or duplicate without individual tracing |

## What this inventory does not yet cover

`chef_battle/arena_snapshot.py` and the 12 adjacent-subsystem files listed in
the last row were not traced to the same evidence depth as the core
visibility/voting/battle-lifecycle files above, given the scope and time
available for this pass. None is classified as dead code or a duplicate —
per the protocol's evidence standard, insufficient evidence means UNKNOWN,
not a promotion to a stronger claim.
