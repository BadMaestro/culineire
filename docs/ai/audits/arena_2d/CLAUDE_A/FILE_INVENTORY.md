# CLAUDE_A — File Inventory (Frontend and Presentation Audit)

Base commit: `2a28e4b2`. All paths relative to repo root. Evidence gathered by direct
Read/Grep of the repository and one Explore sub-search on 2026-07-20; no assumptions
from filename or age alone (see `dead_code_evidence_standard`).

## A. Public Arena floor — the live rendering chain (`/chef-battle/arena/`)

| File | Purpose | Loaded/called by | Calls/loads | Current responsibility | 3D-specific | Reusable | Duplicate candidate | Dead-code status | Evidence | Recommended future action |
|---|---|---|---|---|---|---|---|---|---|---|
| `templates/chef_battle/arena.html` (184 ln) | Page shell: command-deck header, phase rail, metrics, floor section, crown ladder, gifts, crowd rail | `chef_battle/urls.py:12` (`name="arena"`) → `chef_battle/views.py:1135` | `_arena_deck_svg.html`, `_arena_render_ring.html`; CSS: `chef_battle.css`, `arena.css`, `arena_command_deck.css`, `arena_deck_polish.css`, `arena_effects.css`, `arena_hall.css` | Real page shell, binds `arena_data`/`rank_groups`/`active_battle` context directly | No | Yes | No | Live (confirmed via `chef_battle/tests.py:2406` asserting the response contains "arena_render" and NOT "js/arena_puzzle") | KEEP_AND_RESTYLE — structure/context contract stays, visual layer is exactly what the 2D rebuild replaces |
| `templates/chef_battle/_arena_deck_svg.html` (33 ln) | Inline `<svg>` sprite defs for HUD icons (eye/vote/gift/crown/toque/VS), gold gradient | `arena.html:19` | none | Icon symbol library, `currentColor`-based | No | Yes | No | Live | KEEP_AND_REUSE — icons are palette-neutral, reusable as-is in a 2D HUD |
| `templates/chef_battle/_arena_render_ring.html` (88 ln) | The actual `<svg id="arena-render">` mount point, tooltip markup, battle popup shell, crowd-face asset list, script/CSS tags | `arena.html:120`, also `arena_master_console.html:89` | `arena_render.css`; scripts `arena_geometry.js`, `arena_deck.js`, `arena_battle_room.js`, `arena_render.js` (in this load order) | Single rendering surface mount + all interaction-state DOM (tooltip, popup) | No (own markup); yes for what it loads (see below) | Yes, mostly | No | Live. Comment (l.2-4) confirms it is "the merge of the legacy ring (`_arena_ring.html`) and the procedural prototype (`_arena_proto_ring.html`)" — both predecessor files are gone from the repo (deleted at `b17508d3`, "full arena merge phase 2") | KEEP_AND_REUSE for the mount/tooltip/popup DOM contract; the SVG's *content* (what `arena_render.js` draws into it) is where the 2D/3D boundary lives |
| `static/js/arena_geometry.js` (82 ln) | Pure ring/segment math (`get_arena_geometry`-equivalent client-side) | `_arena_render_ring.html:85` | none (pure module) | Ring/segment layout math shared by render + deck | No | Yes | No | Live | KEEP_AND_REUSE — geometry math is presentation-agnostic |
| `static/js/arena_deck.js` (358 ln) | Command-deck DOM wiring (phase rail, metrics, deadline countdown, crown ladder/gifts list refresh) | `_arena_render_ring.html:86` | reads `arena_data` JSON script | HUD/panel state binding, independent of SVG camera model | No | Yes | No | Live | KEEP_AND_REUSE — this is the real-data HUD layer the 2D rebuild should keep exactly |
| `static/js/arena_battle_room.js` (208 ln) | Battle popup open/close, backdrop click-to-close, popup content fetch | `_arena_render_ring.html:87` | `arena_battle_popup.html` fragment via fetch, `#arena-battle-popup`/`#arena-popup-backdrop` DOM | Popup lifecycle, independent of ring camera model | No | Yes | No | Live | KEEP_AND_REUSE |
| `static/js/arena_render.js` (1,054 ln) | Draws the SVG rings/cells/spectators/crowd, projector/camera math, crowd billboard correction, backdrop image sizing/positioning, tooltip fill, `arena/state`+`arena/ping` polling | `_arena_render_ring.html:88` | `#arena-render` SVG, `#arena-tooltip`, `window.ARENA_VIEWER`, `window.ARENA_CROWD_FACES` | THE single active renderer of the arena floor today | **Yes, in part** — see `EVIDENCE.md` §1 for the exact functions/blocks (`CONVERGENCE`, `billboardFaces()`, `placeBackdrop()`, `--arena-backdrop-*`, `.has-arena-backdrop`) that exist specifically to make a flat SVG match a photographic hall background; this is presentation infrastructure for a look the owner has now moved away from, even though `CONVERGENCE` itself is already 0 (no tilt) | Partially — see split below | No | Live, and NOT dead: `billboardFaces()`/backdrop code is actively gated by a class toggle, not orphaned | **KEEP_BACKEND_REPLACE_PRESENTATION** — the ring/cell/occupant placement logic, click/tooltip wiring, and polling loop are reusable; the camera/backdrop-matching subsystem (see EVIDENCE.md) is presentation-only and should not carry into the 2D rebuild as-is |
| `static/css/arena_render.css` (517 ln) | Visual styling for the SVG render: rings, occupant photos, tooltip, popup, crowd figures, backdrop-mode overrides | `_arena_render_ring.html:83` | `hall-bg-v3-final.webp` (l.419) | Paired 1:1 with `arena_render.js` | **Yes, in part** — `.has-arena-backdrop` block (l.496-513), `--arena-backdrop-*` consumption (l.421-422), and the extensive comments (l.337-447) explicitly documenting the retired perspective/tilt model | Partially | No | Live | Same split as `arena_render.js` — ring/cell/tooltip/popup rules keep; backdrop-matching rules are presentation-only |
| `static/css/arena_command_deck.css` (310 ln) | HUD grid layout: header, phase rail, metrics, floor section grid, ladder/gift panels, crowd rail | `arena.html:12` | `arena-floor-plate-v1.png` (l.89, `.arena-floor-stage::before`) | Command-deck layout, largely camera-agnostic | Minor — l.236 comment re: "painted floor lands", l.280-281 `backdrop-filter: blur(9px)` (a glass-panel effect on HUD chrome, not scene camera) | Yes | No | Live | KEEP_AND_RESTYLE — grid/panel structure is reusable, values will need a pass once floor proportions change for 2D |
| `static/css/arena_deck_polish.css` (126 ln) | Follow-up polish pass on command-deck (GB, 2026-07-16) | `arena.html:13` | none | Small supplementary rules layered on `arena_command_deck.css` | Unknown, not fully read | Probable | Possible — two files styling the same deck could indicate un-merged polish; needs a line-by-line diff against `arena_command_deck.css` before calling it a true duplicate | Live | CONSOLIDATE_LATER — candidate for merging into `arena_command_deck.css` once 2D visual pass starts, pending confirmation it doesn't hold unique rules |
| `static/css/arena_effects.css` (132 ln) | Interaction effects layer (GB, 2026-07-16) | `arena.html:14` | none | Click/hover/motion effects scoped to arena | Unknown, not fully read | Probable | Unknown | Live | UNKNOWN_PENDING_RUNTIME_CHECK — read fully before the 2D pass; likely reusable (effects are camera-agnostic) but not verified line-by-line in this audit |
| `static/css/arena_hall.css` (294 ln) | Hall/backdrop-adjacent styling, most recently touched 2026-07-20 (`?v=...-nofunnel`) per its cache-busting suffix | `arena.html:15` | none confirmed by name (not fully read this session) | Per `GOLDEN_RULES.md` history, this was the file at the centre of the "two people editing one file" incident (`arena_hall.css` was Bolt's, GreenBear could not open it) | Likely, given its name and role in the hall-photo era | Unknown | Possible overlap with `arena_render.css`'s backdrop rules | Live | UNKNOWN_PENDING_RUNTIME_CHECK — highest-priority file to read in full before any 2D work begins, given its history as a two-owner conflict point and its name suggesting hall/backdrop presentation |
| `static/images/chef_battle/arena/hall-bg-v3-final.webp` | The one live painted-hall background image | `arena_render.css:419` | — | Live asset | Yes — this is the photographic 3D-look asset itself | N/A (asset) | No | Live | THREE_D_PRESENTATION_ONLY by definition — a photographic hall backdrop is exactly the kind of asset the 2D direction removes |
| `static/images/chef_battle/arena/arena-floor-plate-v1.png` | Floor texture plate | `arena_command_deck.css:89` | — | Live asset (floor `::before` background) | Possibly — depends on whether it depicts a flat tileable texture or a perspective plate; not visually inspected this session | N/A | No | Live | UNKNOWN_PENDING_RUNTIME_CHECK — view the image before classifying; texture plates can be perfectly 2D-compatible |

## B. Orphaned / unreferenced assets found alongside the live chain

| File | Purpose | Loaded/called by | Dead-code status | Evidence | Recommended future action |
|---|---|---|---|---|---|
| `static/images/chef_battle/arena/hall-bg-v1.webp` | Earlier hall-background version | **None found** — zero references in any `.css`/`.js`/`.html` | DEAD_CODE_CANDIDATE (not CONFIRMED — image references can also live in migrations/admin/management commands, not fully checked) | Explore agent's grep found zero hits across static/templates | REVIEW_FOR_REMOVAL once CODEX confirms no backend/fixture/migration reference |
| `static/images/chef_battle/arena/hall-bg-v2-plan.webp` | Mid version, "plan" | **None found** | DEAD_CODE_CANDIDATE | Same as above | REVIEW_FOR_REMOVAL, same caveat |
| `static/js/arena_octant_prototype.js` (103 ln) | Standalone procedural-grid prototype ("Derive procedural arena capacity from geometry segments", per its own git log) | Only `docs/chef_battle/prototypes/arena_octant_prototype.html` (a docs-tree prototype page, not a Django template under `templates/`) | DEAD_CODE_CANDIDATE, strong evidence toward CONFIRMED (no Python import, no URL, no production template include, no test dependency found) but full checklist (settings/admin/deployment) not exhaustively re-verified this session | Explore agent grep: zero references outside the one docs prototype HTML; `recipes/views.py:2926-2943` (a moderation "hex-literal cleanup" tracker) lists it as still containing "2 remaining hex" — i.e. it is tracked as an artifact in Bolt's cleanup ledger, meaning it has not been forgotten, just not deleted | REVIEW_FOR_REMOVAL — this is the single clearest dead-code candidate in the whole inventory; final call belongs to the synthesis stage given `recipes/views.py` is tracking it |

## C. Adjacent "arena"-named features that are NOT the public floor (do not touch during 2D rebuild)

| File / feature | Purpose | Evidence it is a distinct feature | Recommended future action |
|---|---|---|---|
| `templates/chef_battle/arena_master_console.html` (514 ln), `static/css/arena_master_console.css` (603 ln), `static/js/arena_master_console.js` (601 ln) | Staff-only "Arena Master Console" operator tool (battle control panel) | `chef_battle/views.py:2729`, gated by `operator_is_owner`/`operator_author`; reuses `_arena_render_ring.html` as a display widget (l.89) but is otherwise a separate admin surface | DO_NOT_TOUCH as part of the floor rebuild; it consumes the same render partial so it will visually follow the floor's 2D change automatically |
| `templates/moderation/arena_master_console_plan.html` (59 ln), `static/css/arena_master_console_plan.css` (140 ln), `static/js/arena_master_console_plan.js` (39 ln) | Read-only phase-plan viewer for the AMC build phases (P00-P09 docs) | `recipes/views.py:2629` → renders docs under `docs/chef_battle/arena_master_console/` | DO_NOT_TOUCH, unrelated to floor presentation |
| `templates/chef_battle/live_arena_preview.html` (313 ln), `live_arena_progress.html` (100 ln), `static/css/live_arena.css` (234 ln) | Separate staff "watch it build" preview/progress console, linked from the AMC | `chef_battle/urls.py:83-85`, `chef_battle/views.py:3039,3120`, uses `_live_arena_svg.html` (its own SVG defs, not `_arena_deck_svg.html`) | DO_NOT_TOUCH; a different feature entirely, despite the name overlap |
| `templates/chef_battle/_live_arena_svg.html` (53 ln) | Icon defs for the above preview tool | `live_arena_preview.html:11` | KEEP_UNCHANGED, out of floor-rebuild scope |
| `static/css/arena.css` (785 ln) | Shared base arena stylesheet, in use since 2026-07-02 (per its cache-bust suffix `?v=20260702b`), loaded by BOTH `arena.html` and `arena_master_console.html` | `arena.html:11`, `arena_master_console.html:12` | Shared file across two features — any 2D-rebuild edit here must be re-verified against the Master Console page too. UNKNOWN_PENDING_RUNTIME_CHECK for exact rule-level split between "still relevant to the public floor" and "only relevant to the console" |
| `static/css/chef_battle.css` — `.battle-arena-hero`/`.battle-arena-card`/`.battle-arena-news` block (l.1559-1777), `.chef-arena-section`/`.chef-arena-rank` (l.2616-2631), `@keyframes arena-teleport-in`/`arena-teleport-flash` + `.arena-cell--teleport-in`/`.arena-teleport-flash` (l.3582-3599), `.site-battle-widget` block with its own `--arena-*` tokens (l.3852+) | Home-page battle news card, author-profile chef-arena widget, teleport-transition keyframes (per `ARENA_HALL_PLAN.md` Stage B choreography), and a floating site-wide battle widget | Distinct components inside the giant shared `chef_battle.css` (4,808 ln total), not part of the `/arena/` page render chain itself | Not in scope for the floor rebuild's file boundary, but flagged for CLAUDE_B: `.site-battle-widget`'s own `--arena-*` custom-property block is a candidate DUPLICATE_CANDIDATE against `base.css`'s documented `--arena-*` namespace (see `RISKS.md`) |

## D. Shared cross-cutting partial

| File | Purpose | Referenced by | Note |
|---|---|---|---|
| `templates/_enter_arena_btn.html` (top-level `templates/`, not `templates/chef_battle/`) | Shared "Enter the Arena" CTA button partial | 26 templates across `templates/chef_battle/` (see FEATURE_MAP.md and the Explore agent's full include list) | KEEP_UNCHANGED — a heavily reused site-wide CTA, unrelated to the floor's camera/presentation model; touching it is out of the 2D-rebuild's blast radius entirely |
