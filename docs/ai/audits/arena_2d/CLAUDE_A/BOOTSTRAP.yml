documentation_bootstrap:
  agent_id: "CLAUDE_A"
  runtime_note: "This agent's project alias is GreenBear; audit protocol requires
    the CLAUDE_A identity for all cross-agent files and communication."
  branch: "audit/arena-2d-claude-a"
  commit: "2a28e4b2c3be0e1baad7340e06aa1f020931e025"
  documents_read:
    - path: "CLAUDE.md"
      relevance: "Project rules, real model field names, hero layout lock (not in scope for this audit but confirms locked files must not be touched)."
    - path: "docs/agents/GOLDEN_RULES.md"
      relevance: "Current binding operating rules for all three agents: prod-only truth, deploy@ only, one file one owner, no unauthorised changes, arena hidden until release, no camera tilt, light floor, colours from scheme only. Directly frames what counts as current-vs-legacy in this audit."
    - path: "docs/agents/memory/project_session_2026_07_20_state.md"
      relevance: "Most recent state snapshot before this audit: prod v2.5.372 (now 2.5.380 after this session), owner froze all work except arena design, current arena is a flat top-down render composited over a painted background image (hall-bg-v3-final.webp), CONVERGENCE=0, full-bleed, floor matched to background within 2% on all 8 corners. Confirms GreenBear/CLAUDE_A owns all arena_*.css, arena_render.js, arena.html."
    - path: "docs/agents/memory/reference_arena_build_board.md"
      relevance: "Describes the live Arena Build board at /recipes/moderation/arena-build-plan/ (ARENA_BUILD_STAGES in recipes/views.py) tracking backend/frontend stage completion — a second, code-level source of current stage status alongside the docs."
    - path: "docs/chef_battle/arena_rebuild_plan.md"
      relevance: "'Mosaic Rebuild Plan' (owner-approved 2026-07-16): describes a command-deck HTML shell around the existing SVG arena, with real data contracts (metrics, phase, deadline, center, crown_ladder, recent_gifts) that must be preserved. Names #arena-puzzle / arena_data / arena_puzzle.js as 'existing SVG is the single rendering surface' AT THE TIME IT WAS WRITTEN (2026-07-16) — needs reconciliation against the actual current template/JS, since arena_render.js appears to have superseded arena_puzzle.js by 2026-07-20 (see conflict noted below)."
    - path: "docs/chef_battle/arena_mockup_spec.md"
      relevance: "Measured spec of an owner-supplied 1280x720 PERSPECTIVE mockup (2026-07-19), calling for a ~56 degree camera tilt, deep stands, gold rim lighting etc. THIS DIRECTION WAS SUBSEQUENTLY REVERSED — the current user instruction (AUDIT.txt, this session) states explicitly: 'The previous cinematic or pseudo-3D Arena direction is no longer the target. The future direction will be a simpler 2D interface.' Treat this document as a record of an abandoned direction, not a current target — flagged as a documentation conflict below."
    - path: "docs/chef_battle/ARENA_HALL_PLAN.md"
      relevance: "'Approved plan' (2026-07-02) for battle-lifecycle choreography (chef relocation, teleport to centre, embedded battle popup). Status per its own header is APPROVED PLAN, not yet built as of that date; needs verification against current chef_battle/views.py and arena_render.js for how much has since landed."
    - path: "docs/chef_battle/ARENA_INTERACTION_PARITY_AUDIT_REPORT.md"
      relevance: "2026-07-01 report on `arena.html` + `_arena_ring.html` + `static/js/arena_puzzle.js` as the live ring/lobby at the time, plus `arena_battle_popup.html` and `battle_detail.html`. This is the clearest evidence that arena_puzzle.js was the renderer as of 2026-07-01; must confirm whether it is still wired to a live route or superseded by arena_render.js (2026-07-16 onward per this agent's own prior-session memory)."
    - path: "docs/chef_battle/live_arena_frontend_audit.md"
      relevance: "GreenBear's own 'Phase 0 Frontend Presence Audit' listing arena.html (ring/lobby), arena_battle_popup.html (compact popup), battle_detail.html (fuller battle room) as the three live arena-adjacent surfaces, with stage-by-stage frontend presence status (page_shell absent, dual_live_stage absent, etc). Written before the mosaic rebuild plan and the hall-bg-v3-final.webp composite; needs re-verification against current code, not accepted as current truth."
    - path: "docs/agents/memory/golden_rule_author_can_visit_arena.md"
      relevance: "This session's committed record of the owner's access rule (any registered author may visit/spectate the arena, voting requires registered-author status). Backend access gate (chef_battle/access.py) was fixed to match this in v2.5.380 (commit visible in git log), just before this audit began. Not a frontend file but affects which users the CLAUDE_A audit should assume can reach the arena route."
  conflicts_found:
    - document_a: "docs/chef_battle/arena_mockup_spec.md (2026-07-19, calls for ~56 degree perspective tilt, deep stands, painted-hall depth)"
      document_b: "AUDIT.txt current_decision (this session, verbatim): 'The previous cinematic or pseudo-3D Arena direction is no longer the target. The future direction will be a simpler 2D interface.'"
      conflict: "arena_mockup_spec.md is a fully-measured implementation target for a tilted 3D camera; the owner has since reversed this direction entirely in favour of 2D. The spec document itself is NOT stale documentation in the sense of being wrong when written — it accurately recorded a real owner mockup at the time — but it is now describing an ABANDONED target. This audit treats every requirement in arena_mockup_spec.md (perspective, tilt, deep stands, gold rim lighting matched to a photographic hall) as a THREE_D_PRESENTATION_ONLY candidate by definition of the current user instruction, not as an open work item."
    - document_a: "docs/chef_battle/arena_rebuild_plan.md (2026-07-16): 'arena_puzzle.js remains owner of SVG cells... #arena-puzzle, #arena-cells, #arena-centre, #arena-tooltip, #arena-battle-popup, and ARENA_VIEWER keep their ids/names.'"
      document_b: "docs/agents/memory/project_session_2026_07_20_state.md (2026-07-20): 'Ключевые константы (static/js/arena_render.js) FLOOR_SHARE кадрирование, CONVERGENCE = 0 камера, BACKDROP_FLOOR_WIDTH/CX/CY посадка фона.' and this agent's own prior-session summary describing arena_render.js/arena_render.css/arena_command_deck.css as the actively-maintained files, with arena_hall.css as a separate file Bolt briefly touched."
      conflict: "The 2026-07-16 rebuild plan names arena_puzzle.js as the persistent rendering surface going forward. By 2026-07-20 the actively maintained renderer referenced in memory is arena_render.js, a different file. Whether arena_puzzle.js was renamed, replaced, or left running alongside arena_render.js as dead code is UNRESOLVED and is the single most important open question for this audit's dead-code/duplicate analysis. Must be settled with direct repository evidence (grep + git log + url reference), not assumption from either document."
  status: "complete"
