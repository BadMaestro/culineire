schema_version: "1.0"
document_type: "phase_00_reuse_matrix"
project: "CulinEire Chef Battles Arena Master Console"
produced_by: "P00 discovery agent"
produced_at: "2026-07-04"
verified_against_commit: "8c74f678 (main)"
columns_source: "01_CAPABILITY_MAP.yaml phase_00_required_output_columns"

# Every entry verified against the current repository on 2026-07-04:
# chef_battle/models.py, services.py, views.py, urls.py, admin.py,
# templates/chef_battle/*, static/js/arena_puzzle.js, presence app.
# Mockup example numbers were NEVER treated as requirements.

panels:

  # ── Panel 1: Arena Control / Battle Flow ─────────────────────────────
  - mockup_panel: "Arena Control (battle flow deck)"
    entries:
      - requested_metric_or_action: "Current battle phase display"
        existing_source: "Battle.status (models.py:133-146, 13 statuses incl. PAUSED, DISPUTED)"
        existing_permission: "public via battle_detail; no operator view"
        existing_tests: "BattleTimerTests, AutoCompleteVotingTests"
        reuse_strategy: "read Battle.status directly; render with get_status_display"
        genuine_gap: "no operator console surface"
        owner_decision: "DG-02 resolved: GreenBear-only force transitions"
        phase_assignment: "P02 (read), P03 (control)"
      - requested_metric_or_action: "Start / advance / end phase buttons"
        existing_source: "services.accept_challenge (152), calculate_battle_result (394), approve_cooking_phase (902), handle_no_show_battles (269), expire_stale_challenges (243)"
        existing_permission: "service-internal; cooking approval is moderator-gated"
        existing_tests: "ChefBattleServiceTests, AutoCompleteVotingTests"
        reuse_strategy: "thin orchestration adapter calling existing services; direct status write ONLY for states no service covers (GreenBear-only)"
        genuine_gap: "audited operator orchestration layer"
        owner_decision: "DG-02 resolved"
        phase_assignment: "P03"
      - requested_metric_or_action: "Emergency Stop"
        existing_source: "Battle.Status.PAUSED exists (models.py:144, migration 0055 deployed); BattleEvent.EventType.OPERATOR_ACTION exists (models.py:327, migration 0054)"
        existing_permission: "none — no view/service exists yet"
        existing_tests: "none"
        reuse_strategy: "new GreenBear-only service: status→PAUSED, terminate LiveStreamSession, log OPERATOR_ACTION BattleEvent, notify chefs"
        genuine_gap: "pause service + paused_at/paused_reason fields + frontend timer freeze"
        owner_decision: "DG-03 resolved: full spec recorded"
        phase_assignment: "P03 (service), P05 (stream disconnect)"
      - requested_metric_or_action: "Battle scheduling / readiness"
        existing_source: "Battle.challenger_ready, opponent_ready, proposed_combat_time, combat_time_confirmed (models.py:177-180); views.battle_set_ready (2291)"
        existing_permission: "participant-only"
        existing_tests: "chef_battle tests (readiness gate, E3)"
        reuse_strategy: "read-only display of readiness fields"
        genuine_gap: "none for read"
        owner_decision: "not needed"
        phase_assignment: "P02"

  # ── Panel 2: Live Battle Monitor ─────────────────────────────────────
  - mockup_panel: "Live Battle Monitor"
    entries:
      - requested_metric_or_action: "Active battle participants, timers, deadlines"
        existing_source: "Battle.start_time/submission_deadline/voting_deadline/end_time; views.arena_battle_popup (749); views.battle_state_poll (1598)"
        existing_permission: "public (flag-gated by chef_battle_guard)"
        existing_tests: "BattleTimerTests"
        reuse_strategy: "reuse popup/state-poll selectors; denser operator read model in P02"
        genuine_gap: "single assembled operator read model"
        owner_decision: "not needed"
        phase_assignment: "P02, P04"
      - requested_metric_or_action: "Active viewer count"
        existing_source: "presence app: PresenceEvent (presence/models.py:8); arena presence: ChefBattleProfile.last_seen_at via arena_ping (views.py:1014)"
        existing_permission: "public write (heartbeat), no read surface"
        existing_tests: "presence app tests"
        reuse_strategy: "filter existing presence data to battle detail URL; do NOT invent a counter"
        genuine_gap: "battle-scoped presence selector"
        owner_decision: "DG-04 resolved: real presence"
        phase_assignment: "P02"
      - requested_metric_or_action: "Battle chat monitor"
        existing_source: "BattleChatMessage (models.py:717); views.battle_chat_send (1771), battle_chat_poll (1795)"
        existing_permission: "authenticated for send; poll public"
        existing_tests: "chat tests in chef_battle/tests.py"
        reuse_strategy: "reuse chat poll selector read-only"
        genuine_gap: "none for read; moderation actions → P05"
        owner_decision: "not needed"
        phase_assignment: "P02 (read), P05 (moderation)"

  # ── Panel 3: Combat Engine ───────────────────────────────────────────
  - mockup_panel: "Combat Engine console"
    entries:
      - requested_metric_or_action: "Rounds, actions, hidden declarations, hit totals, combat log"
        existing_source: "BattleCombatAction (606), BattleRound (635); services.submit_combat_action (604), _resolve_round (657), get_combat_state (787); views.battle_combat_action (1558), battle_state_poll (1598)"
        existing_permission: "participants write; state poll public"
        existing_tests: "combat tests in chef_battle/tests.py"
        reuse_strategy: "get_combat_state as-is for operator read model; NO second combat engine"
        genuine_gap: "operator intervention policy undefined — default read-only"
        owner_decision: "read-only in P04; interventions require new owner decision"
        phase_assignment: "P04"
      - requested_metric_or_action: "Ingredient biathlon (locks, shots)"
        existing_source: "IngredientLock (674), IngredientShot (698); services.place_ingredient_lock (803), fire_ingredient_shot (826), get_biathlon_state (866); views.biathlon/lock/shoot (1651-1698)"
        existing_permission: "participants only"
        existing_tests: "biathlon tests"
        reuse_strategy: "get_biathlon_state as read selector"
        genuine_gap: "none"
        owner_decision: "not needed"
        phase_assignment: "P04"

  # ── Panel 4: Moderation & Safety ─────────────────────────────────────
  - mockup_panel: "Moderation & Safety"
    entries:
      - requested_metric_or_action: "Cooking approval queue"
        existing_source: "BattleEntry.ModerationStatus (8 states, models.py:213-221); services.approve_cooking_phase (902), get_battles_awaiting_cooking_approval (919); views.cooking_moderation (1700), cooking_moderation_approve (1709)"
        existing_permission: "moderator-gated"
        existing_tests: "cooking moderation tests"
        reuse_strategy: "embed/link existing queue; reuse services for approve"
        genuine_gap: "none for approve; flag/pause/end-stream need audit trail"
        owner_decision: "DG-01/DG-03 resolved"
        phase_assignment: "P05"
      - requested_metric_or_action: "Live stream safety (pause/end stream, reports)"
        existing_source: "LiveStreamSession (1228), LiveBroadcast (1273), LiveBroadcastReport (1312), LiveBattleAgreement (1340); admin.py live-stream administration"
        existing_permission: "Django admin only — NO public/operator endpoints exist (verified urls.py)"
        existing_tests: "gap — no focused tests found for stream termination"
        reuse_strategy: "reuse model state machine + admin actions; console adds authorized POST wrappers"
        genuine_gap: "operator endpoints + tests for termination path"
        owner_decision: "DG-03 covers emergency path"
        phase_assignment: "P05"
      - requested_metric_or_action: "Content reports"
        existing_source: "ContentReport (1055); services.submit_content_report (1789); views.content_report_submit (urls.py:53)"
        existing_permission: "authenticated submit; review in admin"
        existing_tests: "content report tests"
        reuse_strategy: "read-only queue view reusing ContentReport"
        genuine_gap: "console review surface"
        owner_decision: "not needed for read"
        phase_assignment: "P05"

  # ── Panel 5: Voting Integrity ────────────────────────────────────────
  - mockup_panel: "Voting Integrity"
    entries:
      - requested_metric_or_action: "Vote totals per participant"
        existing_source: "BattleVote (276); views.battle_vote (1363); services.calculate_battle_result (394)"
        existing_permission: "public vote; totals in battle_detail"
        existing_tests: "ChefBattleAntiAbuseTests, AutoCompleteVotingTests, PermissionTests"
        reuse_strategy: "count queries on BattleVote"
        genuine_gap: "none"
        owner_decision: "not needed"
        phase_assignment: "P06"
      - requested_metric_or_action: "One-vote enforcement evidence"
        existing_source: "BattleVote uniqueness constraints; private VoteIntegrityEvent rejected-attempt evidence; BattleVote.is_suspicious review flag"
        existing_permission: "DB-level"
        existing_tests: "ChefBattleAntiAbuseTests"
        reuse_strategy: "display aggregate rejected-attempt evidence + accepted is_suspicious records + constraint documentation; never include hashes or voter identity"
        genuine_gap: "none"
        owner_decision: "DG-05 resolved"
        phase_assignment: "P06"
      - requested_metric_or_action: "Tie detection"
        existing_source: "calculate_battle_result draw handling (services.py:394)"
        existing_permission: "service-internal"
        existing_tests: "AutoCompleteVotingTests"
        reuse_strategy: "read-only tie indicator: equal non-zero counts; resolution manual GreenBear decision"
        genuine_gap: "tie indicator selector"
        owner_decision: "DG-05 resolved"
        phase_assignment: "P06"
      - requested_metric_or_action: "Suspicious-activity score"
        existing_source: "BattleVote.is_suspicious + moderation_note only — NO score model exists"
        existing_permission: "n/a"
        existing_tests: "n/a"
        reuse_strategy: "display count of is_suspicious votes; do NOT invent a score"
        genuine_gap: "any 'score' beyond a count is out of scope until owner defines it"
        owner_decision: "DG-05: automatic flags only"
        phase_assignment: "P06"

  # ── Panel 6: Economy / Gifts / Artifacts ─────────────────────────────
  - mockup_panel: "Economy, Gifts & Artifacts"
    entries:
      - requested_metric_or_action: "Token wallet balances, transactions"
        existing_source: "TokenWallet (779), TokenTransaction (793), TokenOrder (823), TokenPackage (736), ProcessedTokenStripeEvent (878); services.credit_tokens (935), debit_tokens (954)"
        existing_permission: "owner-of-wallet; Stripe webhook idempotent"
        existing_tests: "TokenOrderVatConsentTests + token tests"
        reuse_strategy: "read-only aggregates over existing ledger"
        genuine_gap: "none for read; NO console token writes"
        owner_decision: "P07 read-first per master plan"
        phase_assignment: "P07"
      - requested_metric_or_action: "Gift flow (viewer gifts, appreciation)"
        existing_source: "ViewerBattleGift (458), AppreciationGift (529); services.send_battle_artifact (1020), send_appreciation_gift (1071)"
        existing_permission: "authenticated senders; wallet-debited"
        existing_tests: "AppreciationGiftCatalogueTests"
        reuse_strategy: "read-only per-battle gift totals"
        genuine_gap: "'net flow' metric needs approved query definition"
        owner_decision: "define net-flow formula with owner in P07"
        phase_assignment: "P07"
      - requested_metric_or_action: "Artifact inventory / drops / rarity"
        existing_source: "Artifact (380), ChefArtifact (409); services.drop_battle_artifacts (1193)"
        existing_permission: "system-driven drops"
        existing_tests: "artifact tests"
        reuse_strategy: "read-only aggregates; rarity % = share of ChefArtifact by Artifact.Rarity"
        genuine_gap: "rarity % formula needs owner sign-off"
        owner_decision: "confirm formula in P07"
        phase_assignment: "P07"

  # ── Panel 7: CBR / LSR / Rewards Governance ──────────────────────────
  - mockup_panel: "CBR/LSR/Rewards Governance"
    entries:
      - requested_metric_or_action: "Reward records (CBR/LSR) status"
        existing_source: "RewardRecord (889), LedgerEvent hash-chained (952), ChefRewardAgreement (1094); services.issue_reward (1232), reverse_reward (1295), check_next_battle_unlock (1370)"
        existing_permission: "service/admin"
        existing_tests: "RewardRecordLifecycleTests, LedgerEventHashChainTests, LedgerEventImmutabilityTests, LSRRewardTests, BattleCompletionLedgerTests"
        reuse_strategy: "READ-ONLY panel + battle report submission; no console issue/reverse"
        genuine_gap: "battle report model (P08 design)"
        owner_decision: "DG-06 resolved: review-and-report; GreenBear-only financial authority"
        phase_assignment: "P08"
      - requested_metric_or_action: "Payout approve/reject"
        existing_source: "PayoutRequest (1148); services.check_payout_eligibility (1528), create_payout_request (1578), approve_payout_request (1640), reject_payout_request (1682), get_chef_payout_statement (1765)"
        existing_permission: "admin review"
        existing_tests: "PayoutEligibilityTests"
        reuse_strategy: "console buttons call existing approve/reject services; rendered ONLY for OWNER_SLUG"
        genuine_gap: "console POST wrappers with audit"
        owner_decision: "DG-06 resolved"
        phase_assignment: "P08"

  # ── Panel 8: Ranks / Crown / Arena Authority ─────────────────────────
  - mockup_panel: "Ranks, Crown & Arena Authority"
    entries:
      - requested_metric_or_action: "Rank distribution, ratings, streaks"
        existing_source: "ChefBattleProfile (14): rating, rank, wins, losses, win_streak, crown fields, is_suspended, fraud/payout flags; services.rank_for_rating (75); views.rankings (1430), hall_of_fame (1760)"
        existing_permission: "public"
        existing_tests: "CrownTests, SuspensionGateTests, FraudGateTests"
        reuse_strategy: "reuse rankings selectors"
        genuine_gap: "none for read"
        owner_decision: "manual crown/rank actions NOT invented — read-only"
        phase_assignment: "P02, P08"
      - requested_metric_or_action: "Console access management (grant/revoke flag)"
        existing_source: "NONE — RecipeAuthor.has_arena_console_access does not exist yet (verified)"
        existing_permission: "n/a"
        existing_tests: "n/a"
        reuse_strategy: "new BooleanField + GreenBear-only grant/revoke UI (NOT Django admin)"
        genuine_gap: "field, migration, access decorator, grant UI, tests"
        owner_decision: "DG-01 resolved: full access model recorded"
        phase_assignment: "P01 (field+gate) or P03"

stale_assumptions_found:
  - "battle_lifecycle.md status table (declared/accepted/...) does NOT match code: real Battle.Status is scheduled/menu_locked/active/awaiting_submissions/revealed/cooking/presentation/voting/completed/ingredient_penalty/paused/cancelled/disputed. Code is authoritative; declared/accepted live on BattleChallenge, not Battle."
  - "01_CAPABILITY_MAP.yaml omitted views.arena_blast (views.py:882) and Battle.Status.DISPUTED — both exist."
  - "P00_DECISIONS.yaml said 'migration 0055 pending' — 0055 is now committed and deployed."
  - "Capability map 'Public/operator endpoints may not exist' for live stream: CONFIRMED — urls.py has no live-stream operator routes; admin-only."
