from django.db import migrations


class Migration(migrations.Migration):
    """
    Stub — recovered from server DB (GreenBear session 2026-07-10, never pushed).
    Original was a data migration seeding chest_moves values.
    Data already exists in production DB — no operation needed.
    """

    dependencies = [
        ('chef_battle', '0063_enrol_bonus_txtype'),
    ]

    operations = [
    ]
